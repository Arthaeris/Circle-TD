# dependencies (only needed for the OPTIONAL production bundle)
node_modules/

# build output (optional bundle; the game also runs unbundled)
dist/

# sandbox scratch files (safe to delete)
_probe/
_syncprobe.txt

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# FUSE temp artifacts (sandbox only)
.fuse_hidden*
